// Vitest stub for Next.js's 'server-only' marker package, which throws when
// imported outside a Next.js server bundle. Tests import server modules
// directly, so the marker is replaced with a no-op here (see vitest.config.ts).
export {}
